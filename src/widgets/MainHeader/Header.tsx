import styles from "./Header.module.css";
const Header = () => {
  return (
    <header className={styles.header}>
      <div className={styles.text}> My own Workspace</div>
    </header>
  );
};
export default Header;
